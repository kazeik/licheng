<import src="../../comm/listview/icontext/index.wxml"></import>

<view class="main">
    <view wx:for="{{cardata}}" wx:key="{{cardata}}" bindtap="itemevent" data-item='{{item}}'>
            <template is="ListViewIconText" data="{{...item}}"></template>
    </view>
</view>